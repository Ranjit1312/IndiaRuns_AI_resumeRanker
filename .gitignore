# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Secrets — BYO key is session-only, never committed
.env
.env.local

# Local scratch / clone
_repo1/
data/sessions/

# Synthetic test résumés — generated by tools/gen_resumes.py; the image-only
# PDFs (~12 MB each) exceed the HF Space 10 MB/file limit. Regenerate locally,
# never commit.
data/synthetic_resumes/

# OS
.DS_Store
Thumbs.db
